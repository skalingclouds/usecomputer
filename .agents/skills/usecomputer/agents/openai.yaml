interface:
  display_name: "Usecomputer"
  short_description: "Repo-specific patterns and workflows for usecomputer"
  default_prompt: "Use the usecomputer repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true